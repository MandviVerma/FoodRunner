var image = new SimpleImage(200,200);
for (var pixel of image.values()){
    var x = pixel.getX();
    var y = pixel.getY();
    if (pixel.getRed() > 250) {
        pixel.setGreen(255);
    }
}
print(image);